# Configuración placeholder de Nginx para Dokploy.
# Se esperaría servir el frontend estático y redirigir /api al backend Django.
# Ejemplo futuro:
#   location / {
#     root /usr/share/nginx/html;
#     try_files $uri /index.html;
#   }
#   location /api/ {
#     proxy_pass http://backend:8000/;
#   }
# Mantener este archivo como referencia; no se utiliza en el pipeline actual.
