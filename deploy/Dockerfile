# Placeholder Dockerfile para la futura imagen multi-stage.
# Etapa prevista:
# 1. Construcción del frontend con Vite desde /frontend.
# 2. Construcción del backend Django (cuando exista) con poetry/pip.
# 3. Etapa final basada en nginx o gunicorn para servir los artefactos.
#
# No implementar todavía para mantener intacto el pipeline actual de GitHub Pages.
